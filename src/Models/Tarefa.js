export default class Tarefa{

    constructor(prazo, prioridade, descricao, status){

        this.prazo = prazo;
        this.prioridade = prioridade;
        this.descricao = descricao;
        this.status = status;
    }
}